{
  "name": "spas-platform",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev:web": "pnpm --filter @spas/web dev",
    "test:rbac": "pnpm build && node scripts/rbac-smoke.mjs",
    "test:system": "pnpm build && node scripts/system-smoke.mjs"
  }
}
